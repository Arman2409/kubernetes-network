apiVersion: v1
kind: Service
metadata:
  name: go-service
spec:
  type: LoadBalancer # Use NodePort if you're running locally (e.g., Minikube)
  selector:
    app: go-deployment
  ports:
  - name: http
    protocol: TCP
    port: 8080 # Port exposed by the service
    targetPort: 8080 